# Tasks

- [x] Define severe-miss headline behavior in the `live-coach` delta spec.
- [ ] Update `advanceMatchStability()` so severe misses bypass EMA history.
- [ ] Sync the current `live-coach` spec after implementation review.
- [ ] Review complete PR diff for matching-state regressions.
- [ ] Record validation honestly; Actions billing/runner failures are not application failures.
